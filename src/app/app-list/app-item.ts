export interface AppItem {
  fileName: string;
  dateModified: string;
  dateExpired: string,
  fileSize: string;
  fileLink: string;
  iconLink: string;
  state: string;
  expiring: string;
  expired: boolean;
}
